#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -c                      use the complement of ARRAY1
# @flag -C                      use the complement of ARRAY1
# @flag --complement            use the complement of ARRAY1
# @flag -d --delete             delete characters in ARRAY1, do not translate
# @flag -s --squeeze-repeats    replace each sequence of a repeated character that is listed in the last specified ARRAY, with a single occurrence of that character
# @flag -t --truncate-set1      first truncate ARRAY1 to length of ARRAY2
# @flag --help                  display this help and exit
# @flag --version               output version information and exit
# @arg string1
# @arg string2

command eval "$(argc --argc-eval "$0" "$@")"