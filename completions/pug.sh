#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @option -p --program <STRING>         The default program to use with pug.
# @option -w --workdir <STRING>         The working directory containing modules.
# @option -t --max-tasks <INT>          The maximum number of parallel tasks.
# @option --data-dir <STRING>           Directory in which to store plan files.
# @option -e --env <STRING>             Environment variable to pass to terraform process.
# @option -a --arg <STRING>             CLI arg to pass to terraform process.
# @option -f --first-page <STRING>      The first page to open on startup.
# @flag -d --debug                      Log bubbletea messages to messages.log
# @flag -v --version                    Print version.
# @option -c --config <STRING>          Path to config file.
# @flag --disable-reload-after-apply    Disable automatic reload of state following an apply.
# @option -l --log-level[info|debug|error|warn] <STRING>  Logging level.

command eval "$(argc --argc-eval "$0" "$@")"