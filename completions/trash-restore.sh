#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -h --help                              show this help message and exit
# @option --print-completion[bash|zsh|tcsh]    print shell completion script (default: None)
# @option --sort[date|path|none]               Sort list of restore candidates by given field (default: date)
# @flag --version
# @arg path                                    Restore files from given path instead of current directory (default: )

command eval "$(argc --argc-eval "$0" "$@")"