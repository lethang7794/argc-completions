#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --top:                    Show a continuously refreshed process list
# @flag --tree:                   Print a process tree
# @flag --debug:                  Print debug logs (if any) after running
# @flag --install:                Install px, ptop and pxtree in /usr/local/bin/
# @flag --no-pager:               Print PID info to stdout rather than to a pager
# @option --sort <cpupercent:>    Order processes by CPU percentage only
# @flag --no-username:            Don't show the username column in px output
# @flag --color:                  Force color output even when piping
# @flag --help:                   Print this help
# @flag --version:                Print version information
# @arg filter-string <filter string>

command eval "$(argc --argc-eval "$0" "$@")"