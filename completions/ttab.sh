#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag -w                 Open new tab in new terminal window.
# @flag -v                 iTerm only: create a new vertical split
# @flag -h                 iTerm only: create a new horizontal split
# @flag -c                 Terminal/iTerm only: do not open any new window or tab, run in the current tab of the current window.
# @flag -i                 Suppress up-front verification of the existence of the target directory specified via the -d option.
# @option -s <settings>    Assign a settings set (profile).
# @option -t <title>       Specify title for new tab.
# @flag -q                 Clear the new tab's screen.
# @flag -g                 Terminal/iTerm only: create in background (don't activate application).
# @flag -G                 Terminal/iTerm: don't activate new tab and create in background.
# @option -d <dir>         Specify working directory; -d '' disables inheriting the current dir.
# @option -l <secs>        Terminal/iTerm only: delay startup command submission; may be preset via env.
# @flag -a                 Terminal | iTerm Open the new tab in the given terminal app on macOS.

command eval "$(argc --argc-eval "$0" "$@")"