#!/usr/bin/env bash
# Automatic generated, DON'T MODIFY IT.

# @flag --ignore-fail-on-non-empty    ignore each failure to remove a non-empty directory
# @flag -p --parents                  remove DIRECTORY and its ancestors; e.g., 'rmdir -p a/b' is similar to 'rmdir a/b a'
# @flag -v --verbose                  output a diagnostic for every directory processed
# @flag --help                        display this help and exit
# @flag --version                     output version information and exit
# @arg directory*

command eval "$(argc --argc-eval "$0" "$@")"